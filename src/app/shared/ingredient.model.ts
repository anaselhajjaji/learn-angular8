export class Ingredient {
    /*
    This can be replaced by adding accessor in constructor
    public name:string;
    public amount:number;*/
    constructor(public name: string, public amount: number) {
        /*
        This can be replaced by adding accessor in constructor
        this.name = name;
        this.amount = amount;*/
    }
}